// This file can be used to declare global types or augment existing ones.
// For example, if a library doesn't have its own @types package.

// If @types/marked is not comprehensive enough or causes issues, 
// you can declare the module here.
// However, typically @types/marked should suffice.
// declare module 'marked';

// If you were using a library without types, e.g. 'my-untyped-lib':
// declare module 'my-untyped-lib' {
//   export function someFunction(arg: string): number;
//   export const someValue: boolean;
// }
